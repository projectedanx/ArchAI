# Architecture AI Repository Documentation

## 1. Repository Identity & Ontological Glossary
**Identity:** Sovereign Cognitive OS Agentic Architecture Platform.
**Core Principle:** Agentic Engineering (treating LLMs as deterministic software artifacts), utilizing Client-Side Agentic Orchestration for architectural governance.
**Mandate:** Eradicate Semantic Saponification via Paraconsistent Annotated Logic (PAL2v), DCCD 15/85 Schema Inversion, and Epistemic Escrow.

**Key Ontology:**
*   **VCP (Verification Co-Processor):** Asynchronous System 2 controller enforcing latent stability via Differentiable Cache Augmentation.
*   **Epistemic Escrow:** Quarantine state (`EscrowStore`) for cognitive contradictions (CFDI > 0.15) pending Human Semantic Mutex Locking.
*   **Symbolic Scar Ratchet:** Immutable constraints injected from resolved failures.
*   **CFRSG:** Conflict-Free Replicated Semantic Graph for persistent, incrementally-updated DAGs.
*   **Rheological Controller:** Enforces Variable Viscosity Prompting (VVP) balancing deterministic (Crystal) and divergent (Cloud) modes.

## 2. Architecture Topology Map
*   **Client-Side Agentic Orchestration:** React SPA (`App.tsx`) manages specialized agents (`services/geminiService.ts`).
*   **Reflexive Repair Loop (ADR-011):** Enforces Logic Violation Reports (LVR) and Failure-Informed Prompt Inversion (F-IPI).
*   **Edge-Tier (ADR-010):** Domain-agnostic Standard API Gateways decoupled from UI rendering logic (SCAR-GATEWAY-DECOUPLE), mapping Backends for Frontends (BFF) 1:1 with clients (SCAR-BFF-BINDING).
*   **Verification Guard (ADR-013):** The VCP continuously tracks Semantic Drift Coefficient (SDC). Latent excursions ($\xi \ge 0.30$) trigger Differentiable Cache Augmentation to maintain Affective Latent Space Homeostasis (ALSH).

## 3. CI/CD Pipeline Cartograph
*   **Status:** Orphaned/Missing. No `.github/workflows` or container deployment assets (e.g., Dockerfiles) currently exist in the repository topology.

## 4. Dependency Matrix & Entropy Audit
*   **Stack:** React, Vite, TypeScript, `@google/genai`, Recharts, `lucide-react`, Vitest.
*   **Constraint:** Bricolage over Bureaucracy. Reliance on stable, top-5000 ecosystem packages.
*   **Entropy Defense:** Git pre-commit context hashing pipeline and Zotero PDF ingestion script (`scripts/zotero_ingestion_loop.py`) anchor the Personal Knowledge Corpus (`pkc_manifest.yml`).

## 5. Operational Runbook & Cultural Artifacts Log
*   **Workflow:** strict Red-Green-Refactor cycle using Vitest (`npm run test`).
*   **Execution Commands:** `npm install`, `npm run build`, `npm run dev`.
*   **Rules of Engagement:** Enforce "Adjectival Bounding" (max 2 descriptive adjectives), "Hardware-Forced Physicality," and "Spatial Geometry Mandate" (RCC-8 topological bindings).
*   **Cultural Artifacts:** `AGENTS.md` (Axiomatic execution rules), `DOMAIN_GLOSSARY.md` (Rigid bounding vocabulary), `STATE.md` (Immutable truths).
